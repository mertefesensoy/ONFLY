# 2026-09-15 — Phase E slice 1: ONFLY simulates on MVS

| Field | Value |
|---|---|
| Date | 2026-09-15 |
| Author | Engineer, with the owner deciding scope, judging rule and every SRS text change |
| Phase / gate | **Phase E — MVS MVP**, slice 1 (SRS Section 9.2) |
| Owner decisions relied on | D-254 (Phase E is the scope), D-255 (slice 1 first), D-256 (push to `origin/main`), D-257 (TK5 only, no config changes), D-258 (the plan), D-259 (`srext` first), D-260 (ONFREQ via ONFLYDRV), D-261 (how TX-01's MVS half is judged), D-262 and D-263 (authorised SRS text changes), D-264 (continue into the `path` half), D-265 (the signed rate column) |
| Requirements touched | FR-BAT-01 (STEP1 and STEP2 on MVS), FR-BAT-03, FR-BAT-05, FR-LOD-01…05, FR-SIM-01…09, IR-JCL-01, IR-JCL-02, IR-JCL-03, IR-COM-04, IR-COM-05, IR-TRN-01, NR-03, NR-14, ACC-2, ACC-5 (row 6), TX-01 (MVS half), TX-02 |
| Open items closed | None. TBD-06's seeds-per-rate clause and TBD-13 are untouched |

## 1. Problem / motivation

Every previous ONFLY result on a mainframe stops one step short of the thing
the project exists to do.

| What already ran on MVS | Where |
|---|---|
| SoftFloat 2c, TT-01/32 and TT-02 | Gate G1 (D-119) |
| A network file transported and **verified** | Gate G2 (D-141, D-150) |
| The Appendix C kernel against the oracle | `tools/mvsker.py` (D-121, D-122) |
| ONFLYDRV compiled, linked and run in MODE=REQ | Gate G4 (D-161) |

None of them is a simulation driven by a request. `tools/mvseng.py` says so at
the point where it chooses what to link:

> The kernel, the RNG and the stimulus are deliberately absent: verify-only
> never simulates, so linking them would add two job steps each to prove
> nothing.

The consequence was not merely a missing feature. **Row 6 of the SRS Section
8.3 determinism matrix — TK5 MVS 3.8j / SOFT2C / GCCMVS — had never been
filled**, and ACC-5 is defined as each golden request producing the same
fingerprint in *every* row. So ACC-5 was not a mainframe claim at all. And
**TX-01 stood PARTIAL** since Phase D closed, for the reason D-219 and VL-83
recorded: Section 8.5 defines it against MVS records and there were none to
compare.

## 2. What changed

| File | Change |
|---|---|
| `tools/mvsrun.py` | **New.** Phase E slice 1's two MVS jobs: ONFLYDRV in MODE=REQ writing ONFREQ, and GCCMVS building the *simulating* engine and running it. Also the code-page-aware comparison D-261 defines. |
| `tests/run_mvsrun.py` | **New.** 18 cases judging everything about those jobs that can be judged without a mainframe, plus the recorded TK5 result. |
| `tools/mvsbld.py` | `build()` gains `post=()`, whole cards for steps after GO. Needed because the IDCAMS dump of what ONFLYENG wrote is a *step*, not a DD, so it cannot travel in `go_dd`. |
| `tools/mvscob.py` | `deck()` gains `cards`, `req_dsn`, `job` and `title`; `expected_records()` gains `cards`. Phase E needs Gate G4's COBOL job over different control cards and into a different dataset. |
| `cobol/ONFLYDRV.cbl` | The rate column becomes signed (D-265), so that G-13 reaches the engine. |
| `generated/ONFLYDRV.cbl` | Regenerated by `layout/generate.py`. Never edited by hand (IR-COM-01, D-18). |
| `tests/run_cob.py` | Three rejection cases for D-265, and the whole Section 8.4 `path` deck compared against `tools/mkreq.py`'s packing. |
| `Makefile` | `mvsrun` target, wired into `test`. |
| `docs/ONFLY-SRS.md` | D-254…D-265 in Appendix A.1; VL-91 in Appendix D; the authorised text changes to Section 8.5's TX-01 row, Section 8.3's row 6 and Phase D's row in Section 9.2. |
| `data/phase-e/mvs/` | **New.** The recorded TK5 artifacts and the job listings they came from. |

## 3. Implementation approach

### 3.1 Two jobs, and why not one

```
ONFPREQ / ONFEREQ    ONFLYDRV, MODE=REQ, Section 8.4 control cards
                     -> HERC01.ONFLY.{P,E}REQ  (RECFM=FB,LRECL=412)
                     -> IDCAMS PRINT DUMP of what it wrote

ONFPRUN / ONFERUN    GCCMVS compiles and links 13 translation units
                     -> GO: ONFNET on the card reader, ONFREQ, ONFRSP
                     -> IDCAMS PRINT DUMP of what the engine wrote
```

That is FR-BAT-01's STEP1 and STEP2 performed for real, one job each. They are
separate because ONFREQ must exist as a **catalogued** dataset before the
engine's GO step allocates it, and because a COBOL compile deck and a C compile
deck share nothing — one job carrying both would be about 9,000 cards in which
a failure in either half stops the other.

The split also isolates the risk the owner accepted in D-260. ONFREQ is built
by ONFLYDRV rather than transported, so a defect in the driver could masquerade
as a defect in the engine. Dumping and comparing ONFREQ **before** the engine
reads it attributes such a defect at the moment it happens.

### 3.2 What the simulating link adds

`tools/mvseng.py`'s verify-only list, plus five units:

```
SF2C ONFFP2C ONFFPCC ONFFPRC ONFCRCC ONFDECC
     ONFRNDC ONFSTMC ONFKERC ONFREQC ONFCOMC      <- the five
                                     ONFI32C ONFLYENG
```

Link order is stated once, as `UNIT_MEMBERS`, and `run_deck()` asserts the
sources it builds match it. Four of the five had already been compiled on MVS
by `tools/mvssyn.py`, in this order and under these member names; only
`onfreq.c` — D-221's request loop — had never reached a mainframe.

### 3.3 ONFRSP is temporary, ONFREQ is not

ONFREQ crosses a job boundary and must be catalogued; its job deletes and
reallocates it each run, which is what makes the pair rerunnable. ONFRSP does
not cross a boundary, so it travels as `&&ONFRSP` with `DISP=(,PASS)` to the
DUMP step. A catalogued ONFRSP would need scratching first and
`mvsbld.build()` has no hook before its own SCRATCH step; a temporary dataset
needs none and cannot collide with a previous run's leftovers.

### 3.4 The signed rate column (D-265)

`CONV4` turns leading blanks into zeros and then requires four digits (D-159).
G-13 is a rate of −1, which `tools/mkreq.py` packs as a signed halfword
`FFFF`. So the **driver** answered ONF401E and the engine never saw the request
**FR-SIM-06** says it owes ONF202E. Gate G4's row in Section 9.1 had already
named this as Phase E work.

The change accepts a minus where the first non-blank character goes, replaces
it with a zero so the class test still sees four digits, and reports the fact
in `W-NUM-NEG`. The **magnitude is therefore converted by exactly the code that
converted it before** — a separate conversion path for negative numbers would
be a second place for an S0C7 to live.

Two cases needed care:

- A minus that is *not* leading (`12-0`) is already refused, because `W-LEAD`
  is `'N'` by then and the character survives into the class test.
- A minus in column 4 (`   -`) leaves no digits behind it and would otherwise
  convert to `0000` and pass. `W-NUM-SGN` records the column and the test
  refuses that one case.

The sign is honoured **at the call site, not inside `CONV4`**: the rate negates,
the duration refuses. So the duration column behaves exactly as it did before,
and the driver's division of labour with the engine is explicit — the driver
parses the *format*, the engine enforces the *range*.

## 4. Numerical details

No arithmetic changed. Two measured quantities are worth recording.

**Cost per neuron-step.** The `ONFERUN` GO step reported `CPU 13MIN 45.87SEC`
for the five `srext` requests. Each is 1000 ms at a 0.1 ms timestep, so 10,000
steps, over N = 501 neurons:

```
825.87 s / (5 x 501 x 10000)  =  825.87 / 25,050,000  =  32.97 us per neuron-step
```

That is **below** VL-41's measured band of 36.6–40.8 µs, even though this
network carries format v1.1's compensating input, which adds one binary64 add
per neuron per step (Appendix C step 1, D-190). VL-41 was taken on a synthetic
network two days before that table existed, and the two are not comparable
term by term; the number is recorded as what it is, a different measurement on
a different network.

**Where the time goes.** The whole job cost `CPU 14MIN 02.56SEC`, so the
thirteen GCCMVS compiles, the thirteen assemblies and the link together cost
**16.69 s**. On this platform the compiler is free and the simulation is
everything.

**A caution on using 32.97 µs to predict another network.** "Per neuron-step"
is a convenient unit, not a cost model. Appendix C's step does O(1) work per
neuron *plus* work per out-edge of every neuron that fired, so the figure
depends on edge density and on how hard the network is firing, neither of
which is in the denominator:

| Network | N | E | edges per neuron |
|---|---|---|---|
| `srext` | 501 | 10,783 | 21.5 |
| `path` | 913 | 72,852 | 79.8 |

`path` carries **3.7× the edges per neuron**, so 32.97 µs is a **lower bound**
for it and any estimate built on that number is optimistic. This is the same
class of mistake VL-41, VL-42 and VL-43 record three times over: a duration
set from an estimate and refuted by measurement. The figure is reported here
as what it is — one measurement, on one network, on one day.

## 5. Design decisions

### 5.1 The one thing that cannot be byte-identical (D-261)

Section 8.5 defines TX-01 as "Linux s390x and MVS response records are
identical". **Taken literally that is unachievable, and always was.**

`layout/master.py` declares exactly one field of kind `'chr'`, defined in that
file as *"fixed-length alphanumeric, host code page, no translation"*:
`ONF-STIM-CODE`, bytes 0 to 7. Every other field is `'i16'`/`'i32'`,
big-endian on the wire by IR-COM-04, or `'byt'`, opaque. IR-COM-05 already
excludes text from the fingerprint with the stated reason *"so the fingerprint
is identical on ASCII and EBCDIC hosts"* — the design anticipated this for the
**fingerprint** but no requirement said how **TX-01 itself** is judged. Phase D
could not meet the question, because x86 and s390x are both ASCII.

The three candidate rules were **measured** before the owner chose:

| View | Result, all records |
|---|---|
| raw — all 412 bytes | **NO** |
| binary — bytes 8…411, no translation | **YES** |
| translated — bytes 0…7 read as EBCDIC, written as ASCII | **YES** |

D-261 chose **translated identity**. Binary-only was declined for a reason the
test suite now demonstrates rather than asserts: `run_mvsrun.py` case 17 feeds
the comparator records whose stimulus code has been replaced with `XXXXXXXX`
and requires `binary` to pass while `translated` fails. Under the rejected rule
MVS could write anything at all in that field and TX-01 would still pass.

The comparison lives in `tools/mvsrun.py` and **not** in `tests/run_tx.py`.
Phase D's comparator is byte-exact by construction, which is exactly right for
its claim — x86 against s390x, both ASCII — and weakening it to accommodate
Phase E would weaken a claim already made.

### 5.2 Additive changes to shared tools

`mvsbld.build(post=…)` and the keyword arguments on `mvscob.deck()` are both
strictly additive, and `tests/run_mvsrun.py` pins that: cases 11 and 12 require
each function's output with the new arguments defaulted to be identical to its
output without them. Copying either function was the alternative, and it is the
one `tools/mvsbld.py`'s own docstring argues against — every line of those
decks was paid for by a failure, and two copies would drift.

### 5.3 A defect caught before it reached the mainframe

`write_cards()` originally took its defaults in the signature
(`netfile=NETFILE`). Python binds that once, at import, so `select("path")`
changed **where** the card deck was written without changing **which** network
was read: `srext`'s bytes under `path`'s name. That reaches MVS and fails the
payload CRC, and FR-LOD-02's ordering would have attributed it to the
transport. Case 10b exists for it.

## 6. Verification

Every result below was produced in the 2026-09-15 session.

### 6.1 Off-MVS

```
mingw32-make mvsrun
```
→ `run_mvsrun: 18 passed, 0 failed`

```
mingw32-make cob
```
→ `run_cob: 14 records identical to tools/mkreq.py's packing of the same deck,
G-13 rate=-1 (D-265)`, then `run_cob: PASS on x86 GnuCOBOL (cobc (GnuCOBOL)
3.2.0), IBM dialect`.

### 6.2 On TK5 — see section 6.3 for what this does not prove

**Platform for everything in this section:** MVS 3.8j TK5 under Hercules
4.9.1.11612-SDL on this Windows host, Hercules codepage `819/1047` (VL-18),
**GCCMVS 3.2.3 at `-O1`** (the VL-17 pin), float backend **SOFT2C** — the
Release 2c fallback NR-03 puts there, since VL-21 answered "no" for 3e.

**FR-BAT-01 STEP1 — job ONFEREQ.** Every step COND CODE 0000 except `COB`,
which is 0004 (the eight `IKF4072I-W` warnings Gate G4 already recorded).

```
python tools/mvsrun.py --req
  ONF302I STEP SUMMARY: 0005 OK, 0000 ERROR, 0000 ECHOED
  note record 1: raw NO   binary(8..411) yes  translated yes
         chr[0:8] got e2e4c7d940404040 ('SUGR    ' as EBCDIC), want 5355475220202020
  raw=False  binary=True  translated=True
```

**FR-BAT-01 STEP2 — job ONFERUN. The first simulation ONFLY has ever run on
a mainframe.** All thirteen compiles, all thirteen assemblies, `LKED`, `GO`
and `DUMP` at COND CODE 0000:

```
python tools/mvsrun.py --run --out data/phase-e/mvs
  mvsrun: ONFERUN, 8524 cards, 13 translation units, longest 80 columns, GCCMVS -O1
  mvsrun: ONFERUN finished in 855.7 s
  IEF142I ONFERUN COMP1 ... COMP13 / ASM1 ... ASM13 - COND CODE 0000
  IEF142I ONFERUN LKED - STEP WAS EXECUTED - COND CODE 0000
  IEF142I ONFERUN GO   - STEP WAS EXECUTED - COND CODE 0000
  IEF142I ONFERUN DUMP - STEP WAS EXECUTED - COND CODE 0000
  ONF302I STEP SUMMARY: 5 OK, 0 WARN, 0 ERROR
  mvsrun: ONF301I request 1 FP=6C3F7272   ... 5 FP=C4C320BC
```

**TX-01 (MVS half) and ACC-5 row 6.**

```
python tools/mvsrun.py --compare data/phase-e/mvs data/phase-d/x86w
  raw=False  binary=True  translated=True
  ok   G-15  fp=6C3F7272  golden=6C3F7272
  ok   G-16  fp=BAF81D91  golden=BAF81D91
  ok   G-17  fp=F9C7EE77  golden=F9C7EE77
  ok   G-18  fp=4FD0ED1E  golden=4FD0ED1E
  ok   G-19  fp=C4C320BC  golden=C4C320BC
  mvsrun: TX-01 PASS, ACC-5 row 6 PASS
```

G-15 is rate 0 and both readouts come back `lat=-1 spk=0`, so **ACC-2's exact
silence holds on MVS** as well — by construction, since IR-NET-09 requires the
compensating table's rate 0 row to be zero in every neuron.

**Every condition code in the job is 0000** — `grep -c "COND CODE 0000"` over
the saved listing returns **33**, which is exactly SCRATCH, ALLOC, WRITEH,
WRITEC, thirteen COMPs, thirteen ASMs, LKED, GO and DUMP, and
`grep "COND CODE" | grep -v 0000` returns nothing.

**The v1.1 header was decoded by GCCMVS for the first time**, which the
`srext` manifest entry in `data/networks/MANIFEST.json` still listed under
`not_proven`:

```
ONF001I NETWORK LOADED N=501 E=10783 CRC=4577D74E
ONF002I   N               501
ONF002I   BIAS ROWS       9
ONF002I   BYTES NEED      171768 255688
```

`BIAS ROWS 9` is the compensating-input table of D-190 read on MVS, and
**`BYTES NEED ... 255688` is SR-EXT-03's decoded-size figure confirmed by the
target platform's own engine** rather than by the x86 computation the SRS
quotes it from — against TBD-14's measured 8M region (NFR-MEM-01).

**Resource use, from the job listing.** `GO` reported `VIRT 1040K` and
`CPU 13MIN 45.87SEC` — from which §4 derives 32.97 µs per neuron-step, and
16.69 s for the entire compile, assemble and link.

### 6.3 What is NOT proven

- **The `path` half of the golden suite has not run on MVS in this
  section.** Row 6 of the Section 8.3 matrix is filled for `srext` only, and
  FR-BAT-05's ONF201W, D-41's ONF203E and FR-SIM-06's ONF202E have never been
  reached by the MVS engine.
- **Row 7 is untouched.** Nothing here says anything about JCC (VL-03).
- **Four Gate G1 workarounds are still load-bearing and still outside the
  repository**: the `-O1` pin (VL-17), the `819/1047` codepage (VL-18), the
  D-110 amalgamation and the D-111 renames. A checkout cannot reproduce this
  run without them.
- **ONFREQ was built by ONFLYDRV under MVT COBOL (D-260)**, so a defect common
  to the driver and `tools/mkreq.py` would not show here. What bounds that is
  Gate G4's VL-58 and the fact that the records match `req-srext.bin` byte for
  byte after translation — not an independent transport.
- **This is not ACC-6 and not NFR-PERF-01.** 825.87 s is an aggregate over five
  requests. NFR-PERF-01 bounds *one* request at the standard duration, and a
  per-request wall time was not measured. TX-04 remains owed, as VL-90 records.
- **Nothing here is a z/OS or real-hardware result.** Hercules is an emulator;
  VL-01's argument about QEMU applies here in the same form.
- The run took place on a host that was simultaneously running this session's
  x86 builds, so **elapsed** times are not clean measurements. The CPU times
  quoted are Hercules' own accounting, which is what §4 uses.

## 7. Related docs

- SRS Section 8.3 (determinism matrix), 8.4 (golden suite), 8.5 (test catalog),
  9.2 (phases), Appendix A.1 (D-254…D-265), Appendix D (VL-91)
- [2026-09-12 — Gate G2 closed](2026-09-12-gate-g2-closed-transport.md), whose
  card reader this reuses
- [2026-09-12 — Gate G4 closed](2026-09-12-gate-g4-closed-cobol.md), whose
  COBOL job this parameterises
- [2026-09-14 — Phase D](2026-09-14-phase-d-s390x-and-request-loop.md), whose
  recording is the comparand
